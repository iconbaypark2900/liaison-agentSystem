# Templates

Reusable **task prompts and handoffs** you can copy into a new `.spark-flow` task (`spark-flow init`), issue tracker text, or design docs.

## Example

See `example.task.prompt.md`. Duplicate and edit per task.
