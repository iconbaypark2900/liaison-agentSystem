# CLAUDE.md

Read `AGENTS.md` first.

This repo uses `spark-flow`. Follow the current task thread under `.spark-flow/tasks/`.

Default behavior:

- Inspect first.
- Keep changes scoped.
- Do not install dependencies unless explicitly asked.
- Do not use `psutil`.
- Add or update tests.
- Show validation commands.
