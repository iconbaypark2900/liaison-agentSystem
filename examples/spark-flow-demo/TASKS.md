# TASKS.md

## Current task

Build the first working version of `spark-demo-health`.

## Acceptance criteria

- [x] Python package structure exists.
- [x] CLI command `spark-demo-health` is registered.
- [x] CLI prints a readable health report.
- [x] Ollama API errors are handled cleanly.
- [x] Installed Ollama model names can be parsed from `/api/tags` JSON.
- [x] Expected model presence is reported.
- [x] Tests cover model parsing and status classification.
- [x] No runtime dependencies are added.
- [x] `python -m pytest` passes.

## Status

Complete — approved in review, ready for closeout.
