# Examples

This directory contains sanitized examples and demo projects.

Runtime workflow state is intentionally excluded:

- `.spark-flow/`
- logs
- local virtual environments
- generated task outputs

Use examples as reference projects, not as the active control-plane state.
