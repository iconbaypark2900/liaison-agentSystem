# Copy to dashboard/web/.env.local
LIAISON_ROOT=/home/iconbaypark2900/spark/liaison_agentSystem
# Server-side terminal bridge: copy | tmux | wezterm
# When unset, spawn route probes `command -v tmux` and defaults to tmux when present.
# TERMINAL_BRIDGE=tmux
