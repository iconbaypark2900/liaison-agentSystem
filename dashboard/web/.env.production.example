# Copy to dashboard/web/.env.production (or .env.production.local) before scripts/run-dashboard-prod.sh
LIAISON_ROOT=/home/iconbaypark2900/spark/liaison_agentSystem
LIAISON_ENV=PROD
# Same-host Spark/DGX: tmux terminal bridge for POST /api/terminal/spawn
TERMINAL_BRIDGE=tmux
