# Department stub: Planner/Ops liaison

Use this template to define expectations for planners who stage work before BUILD.

- **Cadence**: break work into vertically scoped slices aligned with PROJECT_SPEC acceptance criteria.
- **Hand-offs**: PLAN output must cite allowed/forbidden paths and validation commands consumed by BUILD.
- **Escalations**: routing questions go through `spark-flow capability`/`route`; remote prompts require approve-remote first.
