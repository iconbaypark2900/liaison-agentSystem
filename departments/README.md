# Departments

Markdown or YAML snippets in this folder describe **engineering department roles**: default responsibilities, escalation paths, and which workflows or routes they normally use alongside `config/` and `skills/`.

Treat these as prompts and policy breadcrumbs for orchestration — not executable code.

## Example

See `example.planner_ops.md` for a starter role sketch you can duplicate and rename.
