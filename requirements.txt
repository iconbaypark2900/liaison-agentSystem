# Liaison command center TUI (Textual)
textual>=0.47.0

# Tests (optional — tests/run_smoke.sh also runs __main__ scripts without pytest)
pytest>=8.0.0

# Install: cd $LIAISON_ROOT && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Interactive TUI: .venv/bin/python bin/spark-flow command-center
# Or use bin/liaison-cc wrapper after venv setup
